language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bwimerhcl/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bwimerhcl/docker-react npm run test
